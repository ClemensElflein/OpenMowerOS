# CustomPiOS podman module config
# Written by DocGalaxyBlock (https://github.com/docgalaxyblock/)
# GPL V3
########

# ToDo
# install podman-docker: no / yes
#[ -n "$PODMAN_DOCKER" ] || PODMAN_DOCKER=yes

# install podman-compose: no / stable / devel
[ -n "$PODMAN_COMPOSE" ] || PODMAN_COMPOSE=no